version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-ifood-php
    ports:
      - "8080:80"
    restart: unless-stopped
    volumes:
      - ./ifood-integration/src:/var/www/html